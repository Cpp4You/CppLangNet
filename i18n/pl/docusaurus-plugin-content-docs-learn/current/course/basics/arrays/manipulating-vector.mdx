---
sidebar_position:	2
sidebar_label:		"2. Używanie algorytmów"
title:				Używanie algorytmów
description: 		"Lekcja: manipulowanie vector-ami za pomocą algorytmów w języku C++"
hide_title:			true
---

<!-- Components -->
import SwitchView		from "@site-comps/SwitchView";
import Columns			from "@site-comps/Columns";

<!-- Presets -->
import NotFinished		from "@site/i18n/pl/presets/NotFinished.mdx";

<NotFinished />

# Używanie algorytmów

W bibliotece standardowej języka C++ znajdziemy dużo gotowych algorytmów,
których możemy użyć do manipulowania wektorami.

:::caution Wersja C++
Zalecamy korzystanie z wersji **C++20**, ponieważ rozwiązania z niej są prostsze.
Dla osób, które z jakiegoś powodu nie mogą zainstalować kompilatora, który wspiera
najnowszy standard, zamieścimy też przykłady działające na starszej wersji.
:::

Aby móc skorzystać z algorytmów, musimy dodać następujące nagłówki do kodu:

<SwitchView content={
		{
			"cpp20": <div>

```cpp
#include <ranges>
#include <algorithm>
```

			</div>,
			"until-cpp20": <div>

```cpp
#include <algorithm>
```

			</div>
		}
	}/>

## Sortowanie

Przygotujmy tablicę zawierającą nieposortowane liczby:

```cpp
#include <iostream>
#include <vector>

int main() {
	std::vector<int> numbers = { 5, 3, 1, 4, 2 };
}
```


Aby je posortowac, należy użyć `std::ranges::sort` w ten sposób:

```cpp
#include <iostream>
#include <vector>
#include <ranges>
#include <algorithm>

int main() {
	std::vector<int> numbers = { 5, 3, 1, 4, 2 };

	std::ranges::sort(numbers);
}
```

Wyświetlmy je:

<Columns columns={['8fr', '4fr']}>
<div>

```cpp
#include <iostream>
#include <vector>
#include <ranges>
#include <algorithm>

int main() {
	std::vector<int> numbers = { 5, 3, 1, 4, 2 };

	for (int n : numbers)
		std::cout << n << " ";

	std::ranges::sort(numbers);

	std::cout << '\n';
	for (int n : numbers)
		std::cout << n << " ";
}
```

</div>
<div>

```plaintext title="Result"
5 3 1 4 2
1 2 3 4 5
```

</div>
</Columns>
