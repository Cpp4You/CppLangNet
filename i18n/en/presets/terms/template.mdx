Templates define families of classes (**class template**), functions (**function templates**) and a few more.
They are used to make a function or a class independent from a type.
For example `std::vector` can store elements of type `int` - `std::vector<int>` or `std::string` - `std::vector<std::string>`.