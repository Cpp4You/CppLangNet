This is an alias type defined within the class definition. Example:
```cpp
using pointer			= /* some type */;
using const_pointer	= /* some type */;
```

`pointer` allows to change the pointed-to elements, `const_pointer` doesn't.
