Aspects and semantics that are **implementation-defined** are not guaranteed by
the standard in any way, rather they are left out to the implementation (compiler vendor standard library, most of the times).
For example, most mathematical functions incur some **implementation-defined** behaviour.
