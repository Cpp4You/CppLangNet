**Strong exception guarantee** is a guarantee from the standard library, that if an exception is thrown
in an object's method (for example `std::vector::push_back`) the state of the program is rolled back just before the call
and the object remains valid.