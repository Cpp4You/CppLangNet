This is an alias type defined within the class definition. Example:
```cpp
using iterator        =       /* some type */;
using const_iterator  = const /* some type */;
```

`iterator` allows to change the pointed-to elements, `const_iterator` doesn't.
