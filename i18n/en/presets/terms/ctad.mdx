<small>(Since C++17)</small><br/>
<b>Class template argument deduction</b> (CTAD) enables you to use constructor parameters to deduce types of the class template. Example:  

```cpp
auto x = std::vector{1, 3, 15, 30}; // vector<int>
auto y = std::vector{1.f, 3.f, 15.f, 30.f}; // vector<float>
```