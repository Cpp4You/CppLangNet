In C++ terms, a code that is **ill-formed** should never compile.
For example this code is **ill-formed** because C++ doesn't allow conversions from string literals to `int`s:
```cpp
int i = "Hey!";
```
