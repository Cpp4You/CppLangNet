If a function parameter of a function template is declared as 
an rvalue reference to cv-unqualified *type template parameter* of that same function template,
we call it a *universal reference* or a *forwarding reference*. 

It can bind to both lvalues and rvalues due to a *reference collapsing* rule.