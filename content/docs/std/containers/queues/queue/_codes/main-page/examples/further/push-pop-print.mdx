```cpp title="Push and pop on queue, printing the values"
#include <iostream>
#include <queue>

int main()
{
	std::queue<int> q;

	queue.push(1);
	queue.push(2);
	queue.push(3);

	while (!queue.empty()) {
		std::cout << queue.top() << '\n';
		queue.pop();
	}
}
```