---
title:					deque<...>::front() method
sidebar_label:			front( )
description:			deque<...>::front() method C++ documentation
hide_title:				true
tags:					[access, deque, first, front]

cppreference_origin_rel: w/cpp/container/deque/front
---

import Columns				from "@site-comps/Columns";
import VersionTabs			from "@site-comps/VersionTabs";
import Tooltip				from "@site-comps/Tooltip";

{/* Terms */}
import Term_ContAlias_Reference	from "@site/i18n/en/presets/terms/cont-alias-reference.mdx";
import Term_UndefinedBehaviour from "@site/i18n/en/presets/terms/undefined-behaviour.mdx";

{/* Codes */}

{/* front() */}
import Method_SinceCpp98	from './_codes/front/since-cpp98.mdx';


# std::deque front() method

<VersionTabs content={{
		'since-cpp98': <Method_SinceCpp98 />,
	}}/>

Returns a <Tooltip title={Term_ContAlias_Reference}>reference</Tooltip> to the first element in the container.

:::danger Undefined Behavior
Calling [`front`](#) on an empty container is <Tooltip title={Term_UndefinedBehaviour}>undefined behavior</Tooltip>.
:::

### Parameters

**(none)**

### Return value

Reference to the first element.

### Complexity

Constant - **O(1)**.

### Notes

For a container `c`, the expression `c.front()` is equivalent to `*c.begin()`.

### Example

<Columns columns={['8fr', '4fr']}>
<div>

```cpp title='Main.cpp' showLineNumbers
#include <array>
#include <iostream>
 
int main()
{
    std::deque<char> letters {'o', 'm', 'g', 'w', 't', 'f'};
 
    if (!letters.empty()) {
        std::cout << "The first character is '" << letters.front() << "'.\n";
    }
}
```

</div>
<div>

```plaintext title="Output"
The first character is 'o'.
```

</div>
</Columns>
