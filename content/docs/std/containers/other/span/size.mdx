---
title:					span<...>::size() method
sidebar_label:			size( )
description:			span<...>::size() method C++ documentation
hide_title:				true
tags:					[size, elements, quantity, amount]

cppreference_origin_rel: w/cpp/container/span/size
---

import Columns				from "@site-comps/Columns";
import SwitchView			from "@site-comps/SwitchView";

<!----------------- Codes ---------------------->

<!-- size() -->
import Method_SinceCpp20	from './_codes/size/since-cpp20.mdx';

# std::span size() method

<SwitchView content={{
	'since-cpp20': <Method_SinceCpp20 />,
}} />

Returns the number of elements in the span, i.e. `std::distance(begin(), end())`.

### Parameters

**(none)**

### Return value

The number of elements in the span.

### Complexity

Constant - **O(1)**.

### Notes

Feature testing macro: [`__cpp_lib_ssize`](#).

### Example

<Columns columns={['8fr', '4fr']}>
<div>

```cpp title='Main.cpp' showLineNumbers
#include <iostream>
#include <span>
 
void show_sizes(std::span<const int> span)
{
    std::cout
        << span                 .size() << ' ' // 8
        << span.first(7)        .size() << ' ' // 7
        << span.first<6>()      .size() << ' ' // 6 
        << span.last(5)         .size() << ' ' // 5 
        << span.last<4>()       .size() << ' ' // 4
        << span.subspan(2, 3)   .size() << ' ' // 3 
        << span.subspan<3, 2>() .size() << ' ' // 2
        << '\n';
}
 
int main()
{
    int antique_array[] { 1, 2, 3, 4, 5, 6, 7, 8 };
    show_sizes(antique_array);
}
```

</div>
<div>

```plaintext title="Possible output"
8 7 6 5 4 3 2 
```

</div>
</Columns>

