import VersionTabs	from "@site-comps/VersionTabs";
import { Since }	from "@site-comps/Versions";

The type of the stored elements.

<Since v='cpp17'><span class='inline-danger'>The behaviour is undefined if <code>T</code> is not <code>Container::value_type</code>.</span></Since>
