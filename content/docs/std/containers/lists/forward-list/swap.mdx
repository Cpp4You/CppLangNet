---
title:					forward_list<...>::swap() method
sidebar_label:			swap( )
description:			forward_list<...>::swap() method C++ documentation
hide_title:				true
tags:					[swap, exchange]

cppreference_origin_rel: w/cpp/container/forward_list/swap
---
import Columns		from "@site-comps/Columns";
import VersionTabs	from "@site-comps/VersionTabs";

{/* Codes */}

{/* swap() */}
import Method_SinceCpp17				from './_codes/swap/since-cpp17.mdx';
import Method_SinceCpp11				from './_codes/swap/since-cpp11.mdx';

import NoexceptSpecification_SinceCpp17	from './_codes/swap/noexcept-specification/since-cpp17.mdx';
import PropagateOnContainerSwapNote_SinceCpp11	from './_codes/swap/explanation/propagate-on-container-swap-note-since-cpp11.mdx';

# std::forward_list swap() method

<VersionTabs content={{
	'since-cpp17': <Method_SinceCpp17 />,
	'since-cpp11': <Method_SinceCpp11 />
}}/>


Exchanges the contents of the container with those of `other`.  
Does not invoke any move, copy, or swap operations on individual elements.

:::important
All iterators and references remain valid. It is unspecified whether an iterator holding the past-the-end value in this container will refer to this or the other container after the operation.
:::

<VersionTabs content={{
	'since-cpp11': <PropagateOnContainerSwapNote_SinceCpp11 />
}} />

### Parameters

 - `other` - the container to exchange the contents with

### Return value

**(none)**

### Complexity

Constant - **O(1)**.

### Exceptions

<VersionTabs content={{
	'since-cpp17': <NoexceptSpecification_SinceCpp17 />,
	'until-cpp17': <b>none</b>
}} />

### Example

<Columns columns={['8fr', '4fr']}>
<div>

```cpp title='Main.cpp' showLineNumbers
#include <iostream>
#include <forward_list>
 
template<class Os, class Co> Os& operator<<(Os& os, const Co& co) {
    os << "{";
    for (auto const& i : co) { os << ' ' << i; }
    return os << " } ";
}
 
int main()
{
    std::forward_list<int> a1{1, 2, 3}, a2{4, 5};
 
    auto it1 = std::next(a1.begin());
    auto it2 = std::next(a2.begin());
 
    int& ref1 = a1.front();
    int& ref2 = a2.front();
 
    std::cout << a1 << a2 << *it1 << ' ' << *it2 << ' ' << ref1 << ' ' << ref2 << '\n';
    a1.swap(a2);
    std::cout << a1 << a2 << *it1 << ' ' << *it2 << ' ' << ref1 << ' ' << ref2 << '\n';
 
    // Note that after swap the iterators and references stay associated with their
    // original elements, e.g. it1 that pointed to an element in 'a1' with value 2
    // still points to the same element, though this element was moved into 'a2'.
}
```

</div>
<div>

```plaintext title="Output"
{ 1 2 3 } { 4 5 } 2 5 1 4
{ 4 5 } { 1 2 3 } 2 5 1 4

```

</div>
</Columns>
