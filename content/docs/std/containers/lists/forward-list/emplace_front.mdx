---
title:					forward_list<...>::emplace_front() method
sidebar_label:			emplace_front( )
description:			forward_list<...>::emplace_front() method C++ documentation
hide_title:				true
tags:					[emplace, emplace_front, add]

cppreference_origin_rel: w/cpp/container/forward_list/emplace_front
---

import Columns				from "@site-comps/Columns";
import SwitchView			from "@site-comps/SwitchView";
import Tooltip				from "@site-comps/Tooltip";
import { Since, Until }		from "@site-comps/Versions";

import ImproveSection		from '@site/i18n/en/presets/ImproveSection.mdx';

{/* Terms */}
import Term_UndefinedBehaviour from "@site/i18n/en/presets/terms/undefined-behaviour.mdx";

{/* Codes */}

{/* emplace_front() */}

import Method_SinceCpp11	from './_codes/emplace_front/since-cpp11.mdx';
import Method_SinceCpp17	from './_codes/emplace_front/since-cpp17.mdx';

# std::forward_list emplace_front() method

<SwitchView content={{
	'since-cpp17': <Method_SinceCpp17 />,
	'since-cpp11': <Method_SinceCpp11 />,
}} />

#### Inserts a new element to the beginning of the container.  

The element is constructed through `std::allocator_traits::construct`, which typically uses placement-new to construct the element in-place at the location provided by the container.  
The arguments `args...` are forwarded to the constructor as `std::forward<Args>(args)...`.

{/* todo emplace_front vs push_back */}

### Parameters

 - `args` - arguments to forward to the constructor of the element

### Type requirements

 - `T` *(the container's element type)* must meet the requirements of [`EmplaceConstructible`](/docs/named_req/DefaultInsertable).

### Return value

<Since v='cpp17'>A reference to the inserted element.</Since>
<br />
<Until v='cpp17'><b>(none)</b></Until>


### Complexity

Constant - **O(1)**.

### Exceptions

If an exception is thrown, this function has no effect ([**strong exception guarantee**](#)).

### Example

<ImproveSection />
