import Tooltip from '@site-comps/Tooltip';

import Term_IllFormed from "@site/i18n/en/presets/terms/ill-formed.mdx";

[`CharTraits`](/docs/named_req/CharTraits) class specifying the operations on the character type.  

:::important
Like for [`std::basic_string`](string), `Traits::char_type` must name the same type as `CharT` or the program is <Tooltip title={Term_IllFormed}>ill-formed</Tooltip>.
:::
