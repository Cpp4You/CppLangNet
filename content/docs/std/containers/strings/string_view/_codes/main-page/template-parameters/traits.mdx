[`CharTraits`](/docs/named_req/CharTraits) class specifying the operations on the character type.  

:::danger
Like for [`std::basic_string`](string), `Traits::char_type` must name the same type as `CharT` or the program is ill-formed.
:::
