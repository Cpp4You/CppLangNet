---
title:					string<...>::front() method
sidebar_label:			front( )
description:			string<...>::front() method C++ documentation
hide_title:				true
tags:					[access, first, front]

cppreference_origin_rel: w/cpp/container/string/front
---

import Columns				from "@site-comps/Columns";
import VersionTabs			from "@site-comps/VersionTabs";
import Tooltip				from "@site-comps/Tooltip";

{/* Terms */}
import Term_ContAlias_Reference	from "@site/i18n/en/presets/terms/cont-alias-reference.mdx";
import Term_UndefinedBehaviour from "@site/i18n/en/presets/terms/undefined-behaviour.mdx";

{/* Codes */}

{/* front() */}
import Method_SinceCpp11	from './_codes/front/since-cpp11.mdx';
import Method_SinceCpp20	from './_codes/front/since-cpp20.mdx';

# std::string front() method

<VersionTabs content={{
	'since-cpp20': <Method_SinceCpp20 />,
	'since-cpp11': <Method_SinceCpp11 />,
}} />

Returns a <Tooltip title={Term_ContAlias_Reference}>reference</Tooltip> to the first character in the string.

:::danger Undefined Behavior
Calling [`front()`](#) on an empty string is <Tooltip title={Term_UndefinedBehaviour}>undefined behavior</Tooltip>.
:::

### Parameters

**(none)**

### Return value

Reference to the first character.

### Complexity

Constant - **O(1)**.

### Notes

For a string `str`, the expression `str.front()` is equivalent to `*str.begin()` and `str[0]`.

### Example

<Columns columns={['8fr', '4fr']}>
<div>

```cpp title='Main.cpp' showLineNumbers
#include <iostream>
#include <string>
 
int main()
{
  {
    std::string s("Exemplary");
    char& f = s.front();
    f = 'e';
    std::cout << s << '\n'; // "exemplary"
  }
 
  {
    std::string const c("Exemplary");
    char const& f = c.front();
    std::cout << &f << '\n'; // "Exemplary"
  }
}
```

</div>
<div>

```plaintext title="Output"
exemplary
Exemplary
```

</div>
</Columns>
