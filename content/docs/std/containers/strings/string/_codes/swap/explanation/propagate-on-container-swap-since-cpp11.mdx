import Tooltip		from "@site-comps/Tooltip";

import Term_UndefinedBehaviour from "@site/i18n/en/presets/terms/undefined-behaviour.mdx";

:::danger Undefined Behavior
The <Tooltip title={Term_UndefinedBehaviour}>behavior is undefined</Tooltip> if `Allocator` does not propagate on swap and the allocators of `*this` and `other` are unequal.
:::
