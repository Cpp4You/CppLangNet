<!-- Components -->
import Tooltip					from "@site-comps/Tooltip";

<!-- Presets -->
import Term_CTAD				from "@site/i18n/en/presets/terms/ctad.mdx";

Using <Tooltip title={Term_CTAD}>CTAD</Tooltip>

```cpp
#include <array>

int main() {
	std::array numbers{1, 2, 4, 8};
	// Content: 1,2,4,8

	// ❌ Error, cannot deduce size only (look C++20 to_array)
	// error-next-line
	std::array<int> numbers{1, 2, 4, 8};
}
```