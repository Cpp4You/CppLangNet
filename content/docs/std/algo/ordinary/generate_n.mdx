---
title:         generate_n algorithm
sidebar_label: generate_n(  )
description:   generate_n algorithm C++ documentation
hide_title:    true
tags:          [algorithm, generate_n]

cppreference_origin_rel: w/cpp/algorithm/generate_n
---

import SymbolTable, { Symbol } from "@site-comps/SymbolTable";
import Columns                 from "@site-comps/Columns";
import SwitchView              from "@site-comps/SwitchView";
import Tooltip                 from '@site-comps/Tooltip';
import { Since, Until }        from "@site-comps/Versions";

<!-- Terms -->

import Term_ImplementationDefined from "@site/i18n/en/presets/terms/implementation-defined.mdx";

<!----------------- Codes ---------------------->

import Signature_UntilCpp11 from './_codes/generate_n/until-cpp11.mdx';
import Signature_SinceCpp11 from './_codes/generate_n/since-cpp11.mdx';
import Signature_SinceCpp17 from './_codes/generate_n/since-cpp17.mdx';
import Signature_SinceCpp20 from './_codes/generate_n/since-cpp20.mdx';

# std::generate_n() algorithm

<SwitchView content={{
  'since-cpp20': <Signature_SinceCpp20 />,
  'since-cpp17': <Signature_SinceCpp17 />,
  'since-cpp11': <Signature_SinceCpp11 />,
  'until-cpp11': <Signature_UntilCpp11 />
}} />

- **(1)**
  Assigns values, generated by given function object `g`, to the first `count` elements in the range beginning at `first`, if `count > 0`.  
  Does nothing otherwise.

 - **(2)** Same as **(1)**, but executed according to `policy`.
  :::important Overload Resolution
  These overloads participate in overload resolution only if
  <Until v='cpp20'> std::is_execution_policy_v&lt;std::decay_t&lt;ExecutionPolicy&gt;&gt; </Until> <Since v='cpp20'>std::is_execution_policy_v&lt;std::remove_cvref_t&lt;ExecutionPolicy&gt;&gt;</Since> is <code>true</code>.
  :::

### Parameters

<SymbolTable>

  <Symbol none noLink name={ <code>first</code> }>
    The beginning of the range of elements to generate.
  </Symbol>
  <Symbol none noLink name={ <code>policy</code> }>
    Number of elements to generate.
  </Symbol>
  <Symbol none noLink name={ <code>policy</code> }>
    The execution policy to use. See <a href="">execution policy</a> for details.
  </Symbol>
  <Symbol none noLink name={ <code>g</code> }>

Generator function object that will be called.

The signature of the function should be equivalent to the following:
```cpp
Ret fun();
```
- The type `Ret` must be such that an object of type `OutputIt` can be dereferenced and assigned a value of type `Ret`.


  </Symbol>

</SymbolTable>

#### Type requirements

<SymbolTable>
  <Symbol none noLink name={ <code>OutputIt</code> }>
    <a href="/docs/named_req/LegacyOutputIterator">LegacyOutputIterator</a>
  </Symbol>
  <Symbol none noLink name={ <code>ForwardIt</code> }>
    <a href="/docs/named_req/LegacyForwardIterator">LegacyForwardIterator</a>
  </Symbol>
</SymbolTable>

### Return value

<Since v='cpp11'>Iterator one past the last element assigned if <code>count > 0</code>, <code>first</code> otherwise.</Since> <br/>
<Until v='cpp11'><b>(none)</b></Until>

### Complexity

Exactly `std::max(0, count)` invocations of `g()` and assignments.

### Exceptions

The overloads with a template parameter named `ExecutionPolicy` report errors as follows:
- If execution of a function invoked as part of the algorithm throws an exception and `ExecutionPolicy` is one of the [**standard policies**](#), [`std::terminate`](#) is called.
	For none other `ExecutionPolicy`, the behavior is <Tooltip title={<Term_ImplementationDefined/>}>**implementation-defined**</Tooltip>.
- If the algorithm fails to allocate memory, `std::bad_alloc` is thrown.

### Possible implementation

<details>
<summary>generate_n (1)</summary>

```cpp
template<class OutputIt, class Size, class Generator>
constexpr // since C++20
OutputIt  // void until C++11
generate_n(OutputIt first, Size count, Generator g)
{
    for (Size i = 0; i < count; ++i, ++first)
        *first = g();

    return first;
}
```

</details>

### Examples

<Columns columns={['8fr', '4fr']}>
<div>

```cpp title='Main.cpp' showLineNumbers
#include <algorithm>
#include <functional>
#include <iostream>
#include <iterator>
#include <random>

int main()
{
    std::mt19937 rng; // default constructed, seeded with fixed seed
    std::generate_n(std::ostream_iterator<std::mt19937::result_type>(std::cout, " "),
                    5, std::ref(rng));
    std::cout << '\n';
}
```

</div>
<div>

```plaintext title="Output"
3499211612 581869302 3890346734 3586334585 545404204
```

</div>
</Columns>
