---
sidebar_position: 1
title: Summary
slug: /std/algo/
---

import SwitchView                from  "@site-comps/SwitchView";
import SymbolTable, { Symbol }   from "@site-comps/SymbolTable";
import { Since, Until, Removed } from "@site-comps/Versions";

<!----------------- Codes ---------------------->
import Sorting_Example_Rangified         from "./_codes/main-page/sorting/rangified.mdx";
import Sorting_Example_Ordinary          from "./_codes/main-page/sorting/ordinary.mdx";

import Iterator_Example_Printing         from "./_codes/main-page/operations-with-iterators/printing-vector.mdx";
import Iterator_Example_Printing_Reverse from "./_codes/main-page/operations-with-iterators/printing-vector-in-reverse.mdx";
import Iterator_Example_Zeroing          from "./_codes/main-page/operations-with-iterators/setting-all-numbers-to-0.mdx";

# Algorithms

The C++ algorithm library is a collection of functions that are designed to make it easier to write **correct**,
**efficient**, **expressive** and **reusable** code.

It provides a set of common, highly optimized algorithms that can be used to perform a variety of tasks,
such as searching for an item in a list, sorting a collection of data, or manipulating the contents of a container.


One of the main benefits of using the algorithm library is that it allows you to write code that is easier to understand and less error prone.
All the algorithms have **almost universally recognized names** which helps understanding what certain parts of code using them do without having to do a lot of digging.
Because the algorithms from the standard library have been thoroughly **tested** and **optimized**, you can rely on them to work correctly and efficiently.
This can save you time and effort when developing software and it can help to reduce the risk of introducing bugs into your code.


Another benefit of the algorithm library is that it promotes **code reuse**.
By using the algorithms provided by the library, you can avoid having to reimplement common operations, which can save time and make it easier to maintain your code.


Overall, the C++ algorithm library is an important tool for C++ programmers that makes it easier to write **reliable**, **efficient**, and **reusable** code.

# Usage

To use the algorithms you have to include the `<algorithm>` header, like so:

```cpp
#include <algorithm>
```

Then you can use them in code:

<SwitchView content={{
	'Rangified version': <Sorting_Example_Rangified/>,
	'Ordinary version': <Sorting_Example_Ordinary/>
}}/>

<details>
<summary>Rangified vs ordinary version</summary>
<div>

As you can see in the example above, there are two versions of algorithms. The **rangified** one and the **ordinary** one.
The rangified version came to C++ with the **C++20** standard. If you have a new enough compiler, you should be able to use it.


The basic difference between the two is that the ordinary version only accepts iterators,
while the rangified version is more convenient and allows one to pass the whole range and enables the use of [projections](#).


The rangified version is also **less error-prone**, as you can't pass invalid iterators by mistake (it also handles a few advanced things better than the ordinary version).


All that being said, using the rangified version is more beneficial and convenient, so if you have a possibility of choosing so, prefer the rangified version.

</div>
</details>

<details>
<summary>Ranges and iterators</summary>
<div>

# Iterators

Iteration is the action of repeating a process, in case of containers, **iteration** means traversing the container in a particular order while obtaining each of it's elements.


Iterators are special objects with [pointer](#) semantics (having semantics of something basically means acting like something,
pointer semantics mean that the iterator objects act like pointers in most ways, they can be dereferenced, incremented, etc.. **Pointers are also valid iterators**),
that are used to iterate over containers (like strings, arrays, maps, sets, etc.).


They are obtained via the `begin()` and `end()` functions, and their variants:

 - `cbegin()`/`cend()` - return a **const iterator** to a container (you cannot change the contents via it)
 - `rbegin()`/`rend()` - return a **reverse iterator** to a container (iteration in reverse order)
 - `crbegin()`/`crend()` - return a **const reverse iterator** to a container (iteration in reverse order and you cannot change the contents via it)


Not every container has all the variations, for example [std::unordered_set](/docs/std/containers/sets/unordered-set) doesn't have the `r` and `cr` versions.


Different iterators have different types and they work a little bit differently, but the most basic things you can do with an iterator is:
 - increment it (`it++` and `++it`), which traverses onto the next object in a container
 - dereference it (`*it`), which returns the value that the iterator points to


 Here are a few examples of using iterators with [std::vector](/docs/std/containers/arrays/vector):


<SwitchView content={{
	'Printing vector': <Iterator_Example_Printing/>,
	'Printing vector in reverse': <Iterator_Example_Printing_Reverse/>,
	'Zeroing vector': <Iterator_Example_Zeroing/>,
}}/>

Now, notice how we had to manually specify the type (`std::vector<int>::iterator`, `std::vector<int>::reverse_iterator`, etc.) every time. It is not very convenient. For that purpose you can use the `auto` placeholder:

```cpp
auto begin = numbers.begin();
```
```cpp
auto begin = numbers.rbegin();
```
```cpp
auto begin = numbers.crbegin();
```

`auto` deduces the type for us and it has no performance penalty. The code is easier to read that way, so if you can, prefer it. Read more about it here: [auto](#).

# Ranges

Now what exactly is a range? The exact definition and inner workings in C++ are a little bit complicated, but a range can be defined as anything that we can iterate over. An array, vector, string, map, etc..

All rangified algorithms accept a range. The same way iterators can be different types and have slightly different behaviours, some ranges also differ from others and cannot be used with some algorithms.

</div>
</details>

# List of algorithms

## Searching

<SymbolTable>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/find" name={ <Since v='cpp20'>ranges::find</Since> }>
				Finds the first element satisfying specific criteria.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/find" name="find">
				Finds the first element satisfying specific criteria.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/find_if" name={ <Since v='cpp20'>ranges::find_if</Since> }>
				Finds the first element satisfying specific predicate.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/find_if" name="find_if">
				Finds the first element satisfying specific predicate.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/find_if_not" name={ <Since v='cpp20'>ranges::find_if_not</Since> }>
				Finds the first element **not** satisfying specific predicate.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/find_if_not" name={ <Since v='cpp11'>find_if_not</Since> }>
				Finds the first element **not** satisfying specific predicate.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/find_end" name={ <Since v='cpp20'>ranges::find_end</Since> }>
        Finds the last sequence of elements in a certain range.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/find_end" name="find_end">
        Finds the last sequence of elements in a certain range.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/find_last" name={ <Since v='cpp20'>ranges::find_last</Since> }>
        Finds the last element equal to some value.
			</Symbol>,
	}}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/find_last_if" name={ <Since v='cpp20'>ranges::find_last_if</Since> }>
        Finds the last element satisfying specific criteria.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/find_last_if_not" name={ <Since v='cpp20'>ranges::find_last_if_not</Since> }>
        Finds the last element **not** satisfying specific criteria.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/adjacent_find" name={ <Since v='cpp20'>ranges::adjacent_find</Since> }>
        Finds the first two adjacent items that satisfy a condition.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/adjacent_find" name="adjacent_find">
        Finds the first two adjacent items that satisfy a condition.
      </Symbol>,
  }}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/search" name={ <Since v='cpp20'>ranges::search</Since> }>
        Search for a range of elements.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/search" name="search">
        Search for a range of elements.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/search_n" name={ <Since v='cpp20'>ranges::search_n</Since> }>
        Searches a range for a consecutive sequence of an element.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/search_n" name="search_n">
        Searches a range for a consecutive sequence of an element.
			</Symbol>,
	}}/>

</SymbolTable>

## Determining conditions

<SymbolTable>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/all_of" name={ <Since v='cpp20'>ranges::all_of</Since> }>
				Checks if all elements in a range hold a condition.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/all_of" name={ <Since v='cpp11'>all_of</Since> }>
				Checks if all elements in a range hold a condition.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/any_of" name={ <Since v='cpp20'>ranges::any_of</Since> }>
				Checks if any elements in a range hold a condition.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/any_of" name={ <Since v='cpp11'>any_of</Since> }>
				Checks if any elements in a range hold a condition.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/none_of" name={ <Since v='cpp20'>ranges::none_of</Since> }>
				Checks if no elements in a range hold a condition.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/none_of" name={ <Since v='cpp11'>none_of</Since> }>
				Checks if no elements in a range hold a condition.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/count" name={ <Since v='cpp20'>ranges::count</Since> }>
				Returns the number of certain elements.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/count" name="count">
				Returns the number of certain elements.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/count_if" name={ <Since v='cpp20'>ranges::count_if</Since> }>
				Returns the number of elements satisfying specific criteria.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/count_if" name="count_if">
				Returns the number of elements satisfying specific criteria.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/mismatch" name={ <Since v='cpp20'>ranges::mismatch</Since> }>
        Finds the first position where two ranges differ.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/mismatch" name="mismatch">
				Finds the first position where two ranges differ.
			</Symbol>,
	}}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/starts_with" name={ <Since v='cpp20'>ranges::starts_with</Since> }>
        Checks whether a range starts with another range.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/ends_with" name={ <Since v='cpp20'>ranges::ends_with</Since> }>
        Checks whether a range ends with another range.
      </Symbol>,
  }}/>

</SymbolTable>

## Other non-modifying

<SymbolTable>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/for_each" name={ <Since v='cpp20'>ranges::for_each</Since> }>
				Applies a function to a range of elements.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/for_each" name="for_each">
				Applies a function to a range of elements.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/for_each_n" name={ <Since v='cpp20'>ranges::for_each_n</Since> }>
				Applies a function to a range of elements consisting of an iterator and a size.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/for_each_n" name={ <Since v='cpp17'>for_each_n</Since> }>
				Applies a function to a range of elements consisting of an iterator and a size.
			</Symbol>,
	}}/>

</SymbolTable>

## Random operations

<SymbolTable>

  <SwitchView content={{
    'Ordinary':
      <Symbol linkName="./ordinary/random_shuffle" name={ <Removed v='cpp17'>random_shuffle</Removed> }>
        <span class="inline-danger">Deprecated in C++14 and removed in C++17, use <a href="/docs/std/algo/ordinary/shuffle"><code>std::shuffle</code></a> instead. </span> <br/>
        Randomly re-orders elements in a range.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/shuffle" name={ <Since v='cpp20'>ranges::shuffle</Since> }>
        Randomly re-orders elements in a range.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/shuffle" name={ <Since v='cpp11'>shuffle</Since> }>
        Randomly re-orders elements in a range. <br/>
        Preffered method over <a href="/docs/std/algo/ordinary/random_shuffle"><code>std::random_shuffle</code></a>.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/sample" name={ <Since v='cpp20'>ranges::sample</Since> }>
        Selects <code>n</code> random elements from a sequence.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/sample" name={ <Since v='cpp17'>sample</Since> }>
        Selects <code>n</code> random elements from a sequence.
      </Symbol>,
  }}/>

</SymbolTable>

## Modifying

<SymbolTable>

  <SwitchView content={{
    'Ordinary':
      <Symbol linkName="./ordinary/swap" name="swap">
        Swaps the values of two objects.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/copy" name={ <Since v='cpp20'>ranges::copy</Since> }>
        Copies a range of elements to a new location.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/copy" name="copy">
        Copies a range of elements to a new location.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/copy_if" name={ <Since v='cpp20'>ranges::copy_if</Since> }>
        Copies elements from a range that satisfy a condition to a new location.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/copy_if" name={ <Since v='cpp11'>copy_if</Since> }>
        Copies elements from a range that satisfy a condition to a new location.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/copy_n" name={ <Since v='cpp20'>ranges::copy_n</Since> }>
        Copies a number elements from a range to a new location.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/copy_n" name={ <Since v='cpp11'>copy_n</Since> }>
        Copies a number elements from a range to a new location.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/copy_backward" name={ <Since v='cpp20'>ranges::copy_backward</Since> }>
        Copies a range of elements in backwards order.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/copy_backward" name="copy_backward">
        Copies a range of elements in backwards order.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/move" name={ <Since v='cpp20'>ranges::move</Since> }>
        Moves a range of elements to a new location.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/move" name="move">
        Moves a range of elements to a new location.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/move_backward" name={ <Since v='cpp20'>ranges::move_backward</Since> }>
        Moves a range of elements to a new location in backwards order.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/move_backward" name="move_backward">
        Moves a range of elements to a new location in backwards order.
      </Symbol>,
  }}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/transform" name={ <Since v='cpp20'>ranges::transform</Since> }>
				Applies a function to a range of elements.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/transform" name="transform">
				Applies a function to a range of elements.
			</Symbol>,
	}}/>


	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/reverse" name={ <Since v='cpp20'>ranges::reverse</Since> }>
				Reverses the order of elements.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/reverse" name="reverse">
				Reverses the order of elements.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/reverse_copy" name={ <Since v='cpp20'>ranges::reverse_copy</Since> }>
				Creates a reversed copy of a range.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/reverse_copy" name="reverse_copy">
        Creates a reversed copy of a range.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/fill" name={ <Since v='cpp20'>ranges::fill</Since> }>
        Copy-assigns the given value to every element in a range.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/fill" name="fill">
				Copy-assigns the given value to every element in a range.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/fill_n" name={ <Since v='cpp20'>ranges::fill_n</Since> }>
        Copy-assigns a value to a number of elements.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/fill_n" name="fill_n">
				Copy-assigns a value to a number of elements.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/generate" name={ <Since v='cpp20'>ranges::generate</Since> }>
        Assigns the results of successive function calls to every element in a range.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/generate" name="generate">
				Assigns the results of successive function calls to every element in a range.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/remove" name={ <Since v='cpp20'>ranges::remove</Since> }>
        Removes elements equal to <code>value</code>.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/remove" name="remove">
        Removes elements equal to <code>value</code>.
			</Symbol>,
	}}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/remove_if" name={ <Since v='cpp20'>ranges::remove_if</Since> }>
        Removes elements satisfying specific criteria.
      </Symbol>,
      'Ordinary':
      <Symbol linkName="./ordinary/remove_if" name="remove_if">
        Removes elements satisfying specific criteria.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/remove_copy" name={ <Since v='cpp20'>ranges::remove_copy</Since> }>
        Copies a range of elements omitting those that are equal to <code>value</code>.
    </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/remove_copy" name="remove_copy">
        Copies a range of elements omitting those that are equal to <code>value</code>.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/remove_copy_if" name={ <Since v='cpp20'>ranges::remove_copy_if</Since> }>
        Copies a range of elements omitting those that satisfy a predicate.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/remove_copy_if" name="remove_copy_if">
        Copies a range of elements omitting those that satisfy a predicate.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/replace" name={ <Since v='cpp20'>ranges::replace</Since> }>
        Replaces old values with new values.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/replace" name="replace">
        Replaces old values with new values.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/replace_if" name={ <Since v='cpp20'>ranges::replace_if</Since> }>
        Replaces values satisfying a predicate with new values.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/replace_if" name="replace_if">
        Replaces values satisfying a predicate with new values.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/replace_copy" name={ <Since v='cpp20'>ranges::replace_copy</Since> }>
        Copies a range, replacing elements equal to old value with new value.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/replace_copy" name="replace_copy">
        Copies a range, replacing elements equal to old value with new value.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/replace_copy_if" name={ <Since v='cpp20'>ranges::replace_copy_if</Since> }>
        Copies a range, replacing elements that satisfy a predicate with new value.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/replace_copy_if" name="replace_copy_if">
        Copies a range, replacing elements that satisfy a predicate with new value.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/swap_ranges" name={ <Since v='cpp20'>ranges::swap_ranges</Since> }>
        Swaps two ranges.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/swap_ranges" name="swap_ranges">
        Swaps two ranges.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Ordinary':
      <Symbol linkName="./ordinary/iter_swap" name="iter_swap">
        Swaps the values of the elements the given iterators are pointing to.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/rotate" name={ <Since v='cpp20'>ranges::rotate</Since> }>
        Rotates elements in a range.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/rotate" name="rotate">
        Rotates elements in a range.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/rotate_copy" name={ <Since v='cpp20'>ranges::rotate_copy</Since> }>
        Copies and rotates a range of elements.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/rotate_copy" name="rotate_copy">
        Copies and rotates a range of elements.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/shift_left" name={ <Since v='cpp23'>ranges::shift_left</Since> }>
        Shifts elements in a range left.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/shift_left" name={ <Since v='cpp20'>shift_left</Since> }>
        Shifts elements in a range left.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/shift_right" name={ <Since v='cpp23'>ranges::shift_right</Since> }>
        Shifts elements in a range right.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/shift_right" name={ <Since v='cpp20'>shift_right</Since> }>
        Shifts elements in a range right.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/unique" name={ <Since v='cpp20'>ranges::unique</Since> }>
        Removes consecutive duplicates of an element in a range.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/unique" name="unique">
        Removes consecutive duplicates of an element in a range.
      </Symbol>,
  }}/>

  <SwitchView content={{
    'Rangified':
      <Symbol linkName="./ranges/unique_copy" name={ <Since v='cpp20'>ranges::unique_copy</Since> }>
        Creates a copy of range with no consecutive duplicates.
      </Symbol>,
    'Ordinary':
      <Symbol linkName="./ordinary/unique_copy" name="unique_copy">
        Creates a copy of range with no consecutive duplicates.
      </Symbol>,
  }}/>

</SymbolTable>

## Partitioning

<SymbolTable>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/is_partitioned" name={ <Since v='cpp20'>ranges::is_partitioned</Since> }>
        Determines if the range is partitioned by the given predicate.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/is_partitioned" name={ <Since v='cpp11'>is_partitioned</Since> }>
        Determines if the range is partitioned by the given predicate.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/partition" name={ <Since v='cpp20'>ranges::partition</Since> }>
        Divides a range of elements into two groups based on a predicate.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/partition" name={ <Since v='cpp11'>partition</Since> }>
        Divides a range of elements into two groups based on a predicate.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/partition_copy" name={ <Since v='cpp20'>ranges::partition_copy</Since> }>
        Copies a range dividing the elements into two groups.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/partition_copy" name={ <Since v='cpp11'>partition_copy</Since> }>
        Copies a range dividing the elements into two groups.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/stable_partition" name={ <Since v='cpp20'>ranges::stable_partition</Since> }>
        Divides elements into two groups while preserving their relative order.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/stable_partition" name="stable_partition">
        Divides elements into two groups while preserving their relative order.
			</Symbol>,
  }}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/partition_point" name={ <Since v='cpp20'>ranges::partition_point</Since> }>
        Locates the partition point of a range.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/partition_point" name={ <Since v='cpp11'>partition_point</Since> }>
        Locates the partition point of a range.
			</Symbol>,
  }}/>

</SymbolTable>

## Sorting

<SymbolTable>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/sort" name={ <Since v='cpp20'>ranges::sort</Since> }>
				Sorts a range into (by default) ascending order.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/sort" name="sort">
				Sorts a range into (by default) ascending order.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/stable_sort" name={ <Since v='cpp20'>ranges::stable_sort</Since> }>
				Sorts a range into (by default) ascending order while preserving the order between equal elements.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/stable_sort" name="stable_sort">
				Sorts a range into (by default) ascending order while preserving the order between equal elements.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/partial_sort" name={ <Since v='cpp20'>ranges::partial_sort</Since> }>
				Sorts the first <b>N</b> elements of a range.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/partial_sort" name="partial_sort">
        Sorts the first <b>N</b> elements of a range.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/partial_sort_copy" name={ <Since v='cpp20'>ranges::partial_sort_copy</Since> }>
				Copies and partially sorts a range of elements.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/partial_sort_copy" name="partial_sort_copy">
				Copies and partially sorts a range of elements.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/nth_element" name={ <Since v='cpp20'>ranges::stable_sort</Since> }>
				Partially sorts the given range making sure that it is partitioned by the given element.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/nth_element" name="stable_sort">
				Partially sorts the given range making sure that it is partitioned by the given element.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/is_sorted" name={ <Since v='cpp20'>ranges::is_sorted</Since> }>
				Checks whether a range is sorted.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/is_sorted" name="is_sorted">
				Checks whether a range is sorted.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/is_sorted_until" name={ <Since v='cpp20'>ranges::is_sorted_until</Since> }>
				Finds the largest sorted subrange.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/is_sorted_until" name="is_sorted_until">
				Finds the largest sorted subrange.
			</Symbol>,
	}}/>

</SymbolTable>

## Binary search

<SymbolTable>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/binary_search" name={ <Since v='cpp20'>ranges::binary_search</Since> }>
        Searches for an element in a partially-ordered range.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/binary_search" name="binary_search">
        Searches for an element in a partially-ordered range.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/lower_bound" name={ <Since v='cpp20'>ranges::lower_bound</Since> }>
        Returns an iterator to the first element <em>not less</em> than the given value.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/lower_bound" name="lower_bound">
        Returns an iterator to the first element <em>not less</em> than the given value.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/upper_bound" name={ <Since v='cpp20'>ranges::upper_bound</Since> }>
        Returns an iterator to the first element <em>greater</em> than the given value.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/upper_bound" name="upper_bound">
        Returns an iterator to the first element <em>greater</em> than the given value.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/equal_range" name={ <Since v='cpp20'>ranges::equal_range</Since> }>
        Returns range of elements matching a specific key.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/equal_range" name="equal_range">
        Returns range of elements matching a specific key.
			</Symbol>,
	}}/>

</SymbolTable>

## Other operations on sorted ranges

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/merge" name={ <Since v='cpp20'>ranges::merge</Since> }>
        Merges two sorted ranges.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/merge" name="merge">
        Merges two sorted ranges.
			</Symbol>,
	}}/>

## Set

## Heap

## Min/max

<SymbolTable>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/min" name={ <Since v='cpp20'>ranges::min</Since> }>
				Returns the smallest of the given values.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/min" name="min">
				Returns the smallest of the given values.
			</Symbol>,
	}}/>

	<SwitchView content={{
		'Rangified':
			<Symbol linkName="./ranges/max" name={ <Since v='cpp20'>ranges::max</Since> }>
				Returns the greatest of the given values.
			</Symbol>,
		'Ordinary':
			<Symbol linkName="./ordinary/max" name="max">
				Returns the greatest of the given values.
			</Symbol>
	}}/>

</SymbolTable>

## Comparison

## Permutation

## Numeric

## Operations on uninitialized memory

## C algorithms
