```cpp showLineNumbers title="🔹 Print each number from an array"
#include <iostream>
#include <vector>

int main()
{
  // highlight-start
  std::vector<int> numbers = { 13, 42, -1, 0, -3, -5 };

  for (int n : numbers)
  {
    std::cout << n << ' ';
  }
  // highlight-end
}
```