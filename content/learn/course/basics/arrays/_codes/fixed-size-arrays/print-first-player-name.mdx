```cpp
#include <iostream>
#include <array>
#include <string>

int main() {
	using std::array, std::to_array, std::string;

	auto player_names = to_array<string>({
		"HappyBanana",
		"AngryCrab"
	});

	// Print the name of the first player
	// highlight-next-line
	std::cout << player_names[0] << '\n';
}
```